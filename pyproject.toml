[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-submission"
version = "0.1.0"
dependencies = [
    # List your libraries here, e.g.:
    # "numpy",
    # "requests",
]